export default function Admin() {
  return (
    <section>
      <h1 className="text-3xl font-bold">Admin</h1>
      <p className="mt-2 max-w-2xl text-slate-600">
        Admin console (users, categories, settings) — coming in a future commit.
      </p>
    </section>
  )
}
